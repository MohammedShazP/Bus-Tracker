# session 1

## what I learned

- pyproject.toml is the main configuration file for a modern python project.
- uv.lock records the exact dependency versions for reproducible environments.
- A vitual environment isolates project dependencies.
- Django rest framework provides tools for building rest apis
- Git doesn't track empty directories.
    
